const fs = require("fs")

// use something like taht in each function
const data = fs.readFile("filename","utf-8")
json.parse(data)

// ise something like taht in callback
const apiRequest = new promise((resolve,revoke)=>{
    resolve(result)
})

function getUser(userId, callback) {
    // returns user
}

function getPosts(userId, callback) {
    // returns user's posts
}

function getComments(postId, callback) {
    // returns comments for a post
}

