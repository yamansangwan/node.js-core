// Packages are created by other devs to reuse them

// npm js website conatains packages we can install the packages

// node_modules folder conatains codes of the packages

const catMe = require("cat-me");
console.log(catMe());

// "package.json" maintains the npm packages dependencies

// "package.lock.json" maintains package interconnection dependency , when package depends on another package.